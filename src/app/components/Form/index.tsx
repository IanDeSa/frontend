import { ErrorMessage } from "./ErrorMessage";
import { Field } from "./Field";
import { Input, Textarea, Select, Radio } from "./Input";
import { Label } from "./Label";

export const Form = {
  Label,
  Input,
  Textarea,
  Select,
  Radio,
  ErrorMessage,
  Field,
};
